namespace SPHSimulator.Configuration
{
    /// <summary>
    /// Marker interface for config classes. Used so it can be restricted which classes work with the Configuration File Functionality
    /// </summary>
    public interface IConfig { }
}